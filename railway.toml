# kilo-app (API + لوحة التحكم) — لا تستخدم هذا الملف لخدمة kilo-landing

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/healthz"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
