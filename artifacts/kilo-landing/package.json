{
  "name": "@workspace/kilo-landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build.ts",
    "start": "node src/server.mjs",
    "dev": "pnpm run build && pnpm run start",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workspace/landing-domain": "workspace:*",
    "express": "^5.1.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
