# kilo-landing (www.kilo-sa.com) — عيّن هذا المسار في Railway: Config file = railway.landing.toml

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.landing"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
